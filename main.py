from rocket import Rocket
# Create a rocket with default parameters
my_rocket = Rocket(name="JermTest")

current+timestep = 0
# Create a rocket with custom initial state
rocket2 = Rocket(
    name="FalconStage1",
    position=np.array([0.0, 0.0, 0.0]),
    velocity=np.array([0.0, 0.0, 0.0]),
    thrust_body=np.array([0.0, 0.0, 15000.0]),
    throttle=1.0,
    mass=1200.0
)


while current_time_step < sim_length

    current_time_step += timestep



print("success")